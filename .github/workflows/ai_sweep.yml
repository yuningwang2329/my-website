name: Legacy sweep retained for rollback

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-sweep-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the retired path
        run: |
          echo "The website now mirrors MySecondBrain canonical artifacts."
          echo "The former website-side AI sweep is retained in Git history for rollback only."
          echo "This workflow intentionally does not modify or publish literature data."
